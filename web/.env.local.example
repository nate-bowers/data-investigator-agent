# URL of the FastAPI backend. Local dev default below; set to the deployed Render
# URL in Vercel's project env for production. This is the only public var; no
# secrets live in the frontend bundle.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
